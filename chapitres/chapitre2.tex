\chapter{Réalisation du Sprint 1 : Gestion des utilisateurs et Authentification }

\section{Introduction}

Ce premier sprint a pour objectif de mettre en place le système de gestion des utilisateurs ainsi que les mécanismes de sécurisation de l'application MSG. 
Il constitue la base de la plateforme en assurant l'authentification des différents acteurs, la protection des ressources et le contrôle des accès selon les rôles attribués.

La solution développée repose sur \textbf{Spring Security} et l'authentification par \textbf{JSON Web Token (JWT)} afin de garantir un accès sécurisé aux différentes fonctionnalités de l'application.
Ce sprint couvre également l'inscription des clients, la vérification de l'adresse e-mail, la gestion des mots de passe, la création des employés par l'administrateur ainsi que la gestion des autorisations en fonction des rôles et des départements.

\section{Analyse du Sprint}

Cette section présente l'analyse du premier sprint consacré à la gestion des utilisateurs et à la sécurisation de l'application MSG. Elle décrit les fonctionnalités retenues et les principaux cas d'utilisation réalisés pour répondre aux besoins des différents acteurs du système.

\subsection{Sprint Backlog}

Le Sprint Backlog présente les fonctionnalités sélectionnées sous forme de User Stories, en fonction de leur priorité et de leur importance pour la mise en place de l'authentification, de la gestion des utilisateurs et du contrôle des accès selon les rôles.


\begin{longtable}{|c|>{\RaggedRight\arraybackslash}p{5cm}|>{\RaggedRight\arraybackslash}p{5cm}|c|}
\hline
\textbf{ID} & \textbf{User Story} & \textbf{Critère d'acceptation} & \textbf{Priorité}\\
\hline
\endfirsthead

\hline
\textbf{ID} & \textbf{User Story} & \textbf{Critère d'acceptation} & \textbf{Priorité}\\
\hline
\endhead


US1 &
En tant que système, je souhaite initialiser automatiquement un compte administrateur principal au premier démarrage afin de garantir l'accès initial à la plateforme MSG. &
Le compte Admin est créé automatiquement avec un rôle administrateur et les permissions nécessaires. La création est effectuée uniquement si aucun compte administrateur n'existe dans la base de données. &
Haute
\\
\hline


US2 &
En tant que Customer, je souhaite créer un compte afin d'accéder aux services de l'application MSG. &
Le compte est créé et un e-mail de vérification est envoyé. &
Haute
\\
\hline


US3 &
En tant que Customer, je souhaite vérifier mon adresse e-mail afin d'activer mon compte. &
Le compte est activé après validation du lien reçu par e-mail. &
Moyenne
\\
\hline


US4 &
En tant que Customer, je souhaite me connecter afin d'accéder aux fonctionnalités qui me sont autorisées. &
Une authentification réussie génère un jeton JWT. &
Haute
\\
\hline


US5 &
En tant qu'utilisateur, je souhaite réinitialiser mon mot de passe lorsque je l'ai oublié. &
Un e-mail contenant un lien de réinitialisation est envoyé. &
Moyenne
\\
\hline


US6 &
En tant qu'Admin, je souhaite me connecter à la plateforme afin d'administrer le système. &
L'administrateur est authentifié et accède au tableau de bord d'administration. &
Haute
\\
\hline


US7 &
En tant qu'Admin, je souhaite créer un compte employé afin de lui donner accès à la plateforme. &
Le compte employé est créé avec un rôle et un département puis les identifiants sont envoyés par e-mail. &
Haute
\\
\hline


US8 &
En tant qu'Employee, je souhaite définir un nouveau mot de passe lors de ma première connexion afin de sécuriser mon compte. &
Le mot de passe est modifié et le compte est activé. &
Moyenne
\\
\hline


US9 &
En tant qu'Employee, je souhaite me connecter afin d'accéder aux fonctionnalités correspondant à mon département. &
L'authentification réussie génère un JWT contenant les informations du rôle et du département. &
Haute
\\
\hline


US10 &
En tant qu'Administrateur, je souhaite attribuer un rôle et un département à chaque employé afin de contrôler les autorisations d'accès. &
Les permissions sont appliquées conformément au rôle et au département attribués. &
Moyenne
\\
\hline


US11 &
En tant qu'utilisateur authentifié, je souhaite que mes accès soient contrôlés selon mon rôle afin de protéger les ressources de l'application. &
Les accès sont autorisés ou refusés par Spring Security en fonction des rôles et permissions. &
Haute
\\
\hline


\caption{Sprint Backlog - Gestion des utilisateurs et sécurité}
\label{tab:sprint_backlog}

\end{longtable}


\section{Conception}

Cette section présente la conception détaillée des fonctionnalités développées durant le premier sprint.
Elle décrit les interactions entre les différents composants du système ainsi que la structure des principales entités utilisées pour la gestion des utilisateurs et la sécurisation de l'application MSG.

\subsection{Diagramme de Cas d'Utilisation}

Le diagramme de cas d'utilisation présente les fonctionnalités liées à la gestion des utilisateurs et à la sécurisation de l'application MSG.
Il décrit les interactions entre les différents acteurs du système et les services d'authentification disponibles.

Le système implique trois acteurs principaux :

\begin{itemize}
    \item \textbf{Customer} : peut créer un compte, vérifier son adresse e-mail, se connecter et réinitialiser son mot de passe.
    
    \item \textbf{Admin} : peut se connecter, réinitialiser son mot de passe, créer des comptes Employee et gérer leurs rôles et permissions.
    
    \item \textbf{Employee} : reçoit ses informations de connexion après sa création, définit son mot de passe, se connecte et utilise les fonctionnalités autorisées selon son rôle.
\end{itemize}


\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{images/uml/UseCasAuth.drawio (2).png}
\caption{Diagramme de cas d'utilisation global du Sprint 1}
\label{fig:usecase_sprint1}
\end{figure}
% ============================================================
% DESCRIPTION DES CAS D'UTILISATION
% ============================================================

\section{Description des Cas d’Utilisation}

Afin de compléter le diagramme de cas d’utilisation présenté précédemment, 
les principaux cas d’utilisation du système sont décrits dans les tableaux suivants.

%========================================================
% UC1
%========================================================

\needspace{12cm}
\noindent\textbf{UC1 : Se connecter}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Se connecter \\ \hline

\textbf{Acteur(s)} &
User, Customer, Employee, Admin \\ \hline

\textbf{Préconditions} &
L’utilisateur possède un compte et dispose de ses identifiants de connexion. \\ \hline

\textbf{Scénario} &
1. L’utilisateur accède à la page de connexion. \newline
2. Il saisit son adresse e-mail et son mot de passe. \newline
3. Le système vérifie les informations d’authentification. \newline
4. Le système authentifie l’utilisateur. \newline
5. Le système génère un jeton JWT. \newline
6. L’utilisateur accède aux fonctionnalités correspondant à son rôle. \\ \hline

\textbf{Postconditions} &
L’utilisateur est authentifié et peut accéder aux ressources autorisées selon son rôle et ses permissions. \\ \hline

\textbf{Exceptions} &
Les identifiants sont incorrects, le compte est inactif ou l’utilisateur ne dispose pas des permissions nécessaires. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Se connecter »}
\label{tab:uc1}
\end{table}


%========================================================
% UC2
%========================================================

\needspace{10cm}
\noindent\textbf{UC2 : Oublier le mot de passe}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Oublier le mot de passe \\ \hline

\textbf{Acteur(s)} &
User \\ \hline

\textbf{Préconditions} &
L’utilisateur possède un compte enregistré dans le système. \\ \hline

\textbf{Scénario} &
1. L’utilisateur sélectionne l’option « Mot de passe oublié ». \newline
2. Il saisit son adresse e-mail. \newline
3. Le système vérifie l’existence du compte. \newline
4. Le système lance la procédure de réinitialisation du mot de passe. \\ \hline

\textbf{Postconditions} &
La procédure de réinitialisation du mot de passe est déclenchée. \\ \hline

\textbf{Exceptions} &
L’adresse e-mail n’est associée à aucun compte. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Oublier le mot de passe »}
\label{tab:uc2}
\end{table}


%========================================================
% UC3
%========================================================

\needspace{11cm}
\noindent\textbf{UC3 : Réinitialiser le mot de passe}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Réinitialiser le mot de passe \\ \hline

\textbf{Acteur(s)} &
User \\ \hline

\textbf{Préconditions} &
L’utilisateur a demandé une réinitialisation et dispose d’un lien ou d’un jeton de réinitialisation valide. \\ \hline

\textbf{Scénario} &
1. L’utilisateur accède au lien de réinitialisation reçu par e-mail. \newline
2. Le système vérifie la validité du jeton. \newline
3. L’utilisateur saisit son nouveau mot de passe. \newline
4. Le système valide le nouveau mot de passe. \newline
5. Le système enregistre le nouveau mot de passe. \\ \hline

\textbf{Postconditions} &
Le mot de passe est modifié et peut être utilisé pour une nouvelle connexion. \\ \hline

\textbf{Exceptions} &
Le jeton est invalide ou expiré, ou le nouveau mot de passe ne respecte pas les règles de sécurité. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Réinitialiser le mot de passe »}
\label{tab:uc3}
\end{table}


%========================================================
% UC4
%========================================================

\needspace{10cm}
\noindent\textbf{UC4 : Envoyer un e-mail de réinitialisation}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Envoyer un e-mail de réinitialisation \\ \hline

\textbf{Acteur(s)} &
Système \\ \hline

\textbf{Préconditions} &
Une demande de réinitialisation a été effectuée et l’adresse e-mail correspond à un compte existant. \\ \hline

\textbf{Scénario} &
1. Le système génère un jeton de réinitialisation. \newline
2. Le système construit le lien de réinitialisation. \newline
3. Le système envoie un e-mail contenant le lien à l’utilisateur. \\ \hline

\textbf{Postconditions} &
L’utilisateur reçoit un e-mail contenant le lien de réinitialisation. \\ \hline

\textbf{Exceptions} &
Une erreur peut survenir lors de la génération du jeton ou de l’envoi de l’e-mail. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Envoyer un e-mail de réinitialisation »}
\label{tab:uc4}
\end{table}


%========================================================
% UC5
%========================================================

\needspace{12cm}
\noindent\textbf{UC5 : Créer un employé}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Créer un employé \\ \hline

\textbf{Acteur(s)} &
Admin \\ \hline

\textbf{Préconditions} &
L’administrateur est authentifié et dispose des droits nécessaires pour créer un employé. \\ \hline

\textbf{Scénario} &
1. L’administrateur accède à l’interface de création d’un employé. \newline
2. Il saisit les informations de l’employé. \newline
3. Le système vérifie les données saisies. \newline
4. Le système crée le compte de l’employé. \newline
5. Le système attribue un département à l’employé. \newline
6. Le système envoie les identifiants de connexion par e-mail. \\ \hline

\textbf{Postconditions} &
Le compte de l’employé est créé, son département est attribué et ses identifiants lui sont envoyés. \\ \hline

\textbf{Exceptions} &
Les données saisies sont invalides ou l’adresse e-mail est déjà utilisée. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Créer un employé »}
\label{tab:uc5}
\end{table}


%========================================================
% UC6
%========================================================

\needspace{10cm}
\noindent\textbf{UC6 : Envoyer les identifiants de connexion}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Envoyer les identifiants de connexion \\ \hline

\textbf{Acteur(s)} &
Système \\ \hline

\textbf{Préconditions} &
Un compte employé vient d’être créé et dispose d’une adresse e-mail valide. \\ \hline

\textbf{Scénario} &
1. Le système récupère les informations de connexion. \newline
2. Le système prépare l’e-mail de bienvenue. \newline
3. Le système envoie les informations de connexion à l’employé. \\ \hline

\textbf{Postconditions} &
L’employé reçoit ses informations de connexion par e-mail. \\ \hline

\textbf{Exceptions} &
Une erreur d’envoi de l’e-mail peut survenir. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Envoyer les identifiants de connexion »}
\label{tab:uc6}
\end{table}


%========================================================
% UC7
%========================================================

\needspace{11cm}
\noindent\textbf{UC7 : Attribuer un département}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Attribuer un département \\ \hline

\textbf{Acteur(s)} &
Admin \\ \hline

\textbf{Préconditions} &
L’employé existe dans le système et l’administrateur dispose des droits nécessaires. \\ \hline

\textbf{Scénario} &
1. L’administrateur sélectionne un employé. \newline
2. Il sélectionne le département à attribuer. \newline
3. Le système vérifie l’existence de l’employé et du département. \newline
4. Le système enregistre l’affectation. \\ \hline

\textbf{Postconditions} &
L’employé est associé au département sélectionné. \\ \hline

\textbf{Exceptions} &
L’employé ou le département n’existe pas, ou l’administrateur ne dispose pas des droits nécessaires. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Attribuer un département »}
\label{tab:uc7}
\end{table}


%========================================================
% UC8
%========================================================

\needspace{12cm}
\noindent\textbf{UC8 : Gestion des rôles et des permissions}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Gestion des rôles et des permissions \\ \hline

\textbf{Acteur(s)} &
Admin \\ \hline

\textbf{Préconditions} &
L’administrateur est authentifié et dispose des droits d’administration. \\ \hline

\textbf{Scénario} &
1. L’administrateur accède à l’interface de gestion des rôles et permissions. \newline
2. Il sélectionne un employé. \newline
3. Il attribue ou modifie le rôle et les permissions. \newline
4. Le système vérifie les données. \newline
5. Le système enregistre les modifications. \\ \hline

\textbf{Postconditions} &
Les rôles et les permissions de l’employé sont mis à jour. \\ \hline

\textbf{Exceptions} &
L’administrateur ne dispose pas des droits nécessaires ou les données saisies sont invalides. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Gestion des rôles et des permissions »}
\label{tab:uc8}
\end{table}


%========================================================
% UC9
%========================================================

\needspace{11cm}
\noindent\textbf{UC9 : Changer le mot de passe}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Changer le mot de passe \\ \hline

\textbf{Acteur(s)} &
Employee, User \\ \hline

\textbf{Préconditions} &
L’utilisateur est authentifié ou effectue sa première connexion avec les identifiants reçus. \\ \hline

\textbf{Scénario} &
1. L’utilisateur accède à l’interface de changement du mot de passe. \newline
2. Il saisit son nouveau mot de passe. \newline
3. Le système vérifie le respect des règles de sécurité. \newline
4. Le système enregistre le nouveau mot de passe. \\ \hline

\textbf{Postconditions} &
Le nouveau mot de passe est enregistré et peut être utilisé pour les prochaines connexions. \\ \hline

\textbf{Exceptions} &
Le nouveau mot de passe ne respecte pas les règles de sécurité ou une erreur survient lors de l’enregistrement. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Changer le mot de passe »}
\label{tab:uc9}
\end{table}


%========================================================
% UC10
%========================================================

\needspace{12cm}
\noindent\textbf{UC10 : S’inscrire}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
S’inscrire \\ \hline

\textbf{Acteur(s)} &
Customer \\ \hline

\textbf{Préconditions} &
Le client ne possède pas encore de compte et le formulaire d’inscription est accessible. \\ \hline

\textbf{Scénario} &
1. Le client accède au formulaire d’inscription. \newline
2. Il saisit ses informations personnelles. \newline
3. Le système valide les informations saisies. \newline
4. Le système crée le compte client. \newline
5. Le système envoie un e-mail de vérification. \\ \hline

\textbf{Postconditions} &
Le compte client est créé et reste inactif jusqu’à la vérification de l’adresse e-mail. \\ \hline

\textbf{Exceptions} &
L’adresse e-mail est déjà utilisée ou les informations saisies sont invalides. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « S’inscrire »}
\label{tab:uc10}
\end{table}


%========================================================
% UC11
%========================================================

\needspace{10cm}
\noindent\textbf{UC11 : Envoyer un e-mail de vérification}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Envoyer un e-mail de vérification \\ \hline

\textbf{Acteur(s)} &
Système \\ \hline

\textbf{Préconditions} &
Un compte client vient d’être créé avec une adresse e-mail valide. \\ \hline

\textbf{Scénario} &
1. Le système génère un jeton de vérification. \newline
2. Le système construit le lien de vérification. \newline
3. Le système envoie l’e-mail de vérification au client. \\ \hline

\textbf{Postconditions} &
Le client reçoit un e-mail contenant le lien de vérification de son adresse e-mail. \\ \hline

\textbf{Exceptions} &
Une erreur peut survenir lors de la génération du jeton ou de l’envoi de l’e-mail. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Envoyer un e-mail de vérification »}
\label{tab:uc11}
\end{table}


%========================================================
% UC12
%========================================================

\needspace{11cm}
\noindent\textbf{UC12 : Vérifier l’adresse e-mail}\par
\vspace{0.3cm}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|>{\raggedright\arraybackslash}p{4cm}|
                >{\raggedright\arraybackslash}p{10.5cm}|}
\hline
\textbf{Élément} & \textbf{Description} \\ \hline

\textbf{Cas d’utilisation} &
Vérifier l’adresse e-mail \\ \hline

\textbf{Acteur(s)} &
Customer \\ \hline

\textbf{Préconditions} &
Le compte client existe et un e-mail de vérification a été envoyé. \\ \hline

\textbf{Scénario} &
1. Le client ouvre le lien de vérification reçu par e-mail. \newline
2. Le système vérifie la validité du jeton. \newline
3. Le système valide l’adresse e-mail. \newline
4. Le système active le compte client. \\ \hline

\textbf{Postconditions} &
L’adresse e-mail est vérifiée et le client peut se connecter à son compte. \\ \hline

\textbf{Exceptions} &
Le jeton de vérification est invalide ou expiré. \\ \hline

\end{tabular}
\caption{Description du cas d’utilisation « Vérifier l’adresse e-mail »}
\label{tab:uc12}
\end{table}

\subsection{Diagrammes de Séquence}

Les diagrammes de séquence suivants illustrent le déroulement de deux fonctionnalités
principales liées à la gestion des employés : la création d'un employé par
l'administrateur et sa première authentification.

\subsubsection{Diagramme de Séquence : Création d'un Employé}

Le diagramme suivant décrit le processus de création d'un employé. 
L'administrateur saisit les informations nécessaires depuis l'interface Angular.
La requête est ensuite transmise au backend Spring Boot, où les droits d'accès
et les données sont vérifiés. Le système crée l'employé, son contrat ainsi que
son solde de congés, puis envoie ses identifiants de connexion par e-mail.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{images/uml/seq1.png}
    \caption{Diagramme de séquence - Création d'un employé}
    \label{fig:sequence_employee}
\end{figure}


\subsubsection{Diagramme de Séquence : Première Authentification de l'Employé}

Le diagramme suivant présente le processus d'authentification d'un employé.
Après la saisie de ses identifiants, le système vérifie les informations
d'authentification et génère un jeton JWT. Lors de la première connexion,
l'employé est invité à modifier son mot de passe avant d'accéder au tableau
de bord. En cas d'échec, le système retourne une erreur d'authentification.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{images/uml/seq2.png}
    \caption{Diagramme de séquence - Première authentification de l'employé}
    \label{fig:sequence_first_authentication}
\end{figure}


\subsection{Diagramme de Classes}

Le diagramme de classes présente la structure statique du module de gestion
des utilisateurs. Il décrit les principales classes, leurs attributs ainsi
que les relations qui existent entre elles.

Les principales classes représentées sont :

\begin{itemize}

    \item \textbf{User} : classe principale regroupant les informations
    communes à tous les utilisateurs, telles que l'identité, les informations
    de contact et les données liées à l'authentification.

    \item \textbf{Customer} : représente les clients de la plateforme et
    contient notamment leurs informations de fidélité.

    \item \textbf{Employee} : représente les employés du système et contient
    leurs informations professionnelles, leur disponibilité et leur statut.

    \item \textbf{Admin} : représente les administrateurs responsables de
    la gestion et de l'administration du système.

    \item \textbf{Department} : représente les différents départements
    auxquels les employés peuvent être affectés.

    \item \textbf{Permission} : représente les permissions disponibles
    dans le système et permet de définir les droits associés aux départements.

    \item \textbf{EmploymentStatus} : énumération permettant de représenter
    l'état d'un employé : \textit{ACTIVE}, \textit{ON\_LEAVE},
    \textit{RESIGNED} ou \textit{TERMINATED}.

\end{itemize}

Les principales relations sont les suivantes :

\begin{itemize}

    \item \textbf{Customer}, \textbf{Employee} et \textbf{Admin} héritent
    de la classe \textbf{User} et partagent ainsi les informations communes
    liées aux utilisateurs.

    \item Un \textbf{Employee} est associé à un \textbf{Department}.

    \item Un \textbf{Department} peut être associé à plusieurs
    \textbf{Permission}.

    \item Le statut d'un \textbf{Employee} est défini à l'aide de
    l'énumération \textbf{EmploymentStatus}.

\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{images/uml/diagrammeClasseSprint1.drawio (1).png}
    \caption{Diagramme de classes - Gestion des utilisateurs}
    \label{fig:class_diagram}
\end{figure}

\section{Architecture de Sécurité}

La sécurité de l'application \ac{MSG} repose sur l'intégration de \textit{Spring Security} et du mécanisme d'authentification basé sur le \ac{JWT} (\textit{JSON Web Token}). Cette architecture permet d'assurer l'authentification des utilisateurs, la protection des ressources exposées par les API REST ainsi que le contrôle des accès en fonction des rôles attribués à chaque utilisateur.

Comme illustré par la Figure~\ref{fig:architecture-jwt}, le processus de sécurité débute lors de la connexion de l'utilisateur. Après vérification des informations d'authentification, un jeton JWT est généré puis transmis au client. Ce jeton est ensuite utilisé pour accéder aux ressources protégées, où il est systématiquement vérifié avant l'autorisation de chaque requête.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/architecture/archJWT.png}
    \caption{Architecture de sécurité basée sur Spring Security et JWT}
    \label{fig:architecture-jwt}
\end{figure}

\subsection{Architecture Spring Security}

Spring Security est un framework dédié à la sécurisation des applications Spring. Dans l'application MSG, il prend en charge l'authentification des utilisateurs, la protection des API et le contrôle des autorisations en fonction des rôles.

Son fonctionnement repose principalement sur les composants suivants.

\subsubsection{Security Filter Chain}

La \textit{Security Filter Chain} intercepte chaque requête HTTP avant son traitement par l'application. Elle vérifie la présence et la validité du JWT, identifie l'utilisateur authentifié et autorise ou refuse l'accès aux ressources protégées.

\subsubsection{Authentication Provider}

L'\textit{Authentication Provider} est chargé de valider les informations de connexion. Il recherche l'utilisateur dans la base de données, compare le mot de passe fourni avec celui enregistré et confirme l'identité de l'utilisateur.

\subsubsection{UserDetailsService}

Le composant \textit{UserDetailsService} récupère les informations nécessaires à l'authentification depuis la base de données, notamment l'identifiant, le mot de passe chiffré ainsi que les rôles associés à l'utilisateur.

\subsubsection{PasswordEncoder}

Le \textit{PasswordEncoder} assure le chiffrement des mots de passe avant leur stockage et permet une comparaison sécurisée lors de l'authentification, garantissant ainsi qu'aucun mot de passe n'est conservé en clair.

Les mécanismes de sécurité présentés s'appuient sur les recommandations de Spring Security \cite{springSecurityDoc,springInAction}.

\subsection{Authentification basée sur JWT}

Le \ac{JWT} (\textit{JSON Web Token}) est un standard permettant de transmettre de manière sécurisée les informations d'authentification entre le client et le serveur. Contrairement à une authentification basée sur les sessions, cette approche est \textit{stateless} : le serveur ne conserve pas l'état de connexion des utilisateurs, les informations nécessaires étant contenues dans le jeton.

Le processus d'authentification se déroule selon les étapes suivantes.

\begin{enumerate}
    \item L'utilisateur saisit son adresse e-mail et son mot de passe, qui sont envoyés au serveur.

    \item Les identifiants sont vérifiés à l'aide du \textit{UserDetailsService} et du \textit{PasswordEncoder}. Si les informations sont valides, l'utilisateur est authentifié.

    \item Le service \textit{JwtService} génère un jeton JWT contenant l'identifiant de l'utilisateur, son rôle ainsi que sa date d'expiration.

    \item Lors de chaque requête vers une ressource protégée, le client transmet ce jeton dans l'en-tête HTTP \texttt{Authorization}. Le filtre JWT valide alors le jeton avant de permettre l'accès à la ressource demandée.
\end{enumerate}

Cette approche améliore les performances des applications distribuées en supprimant la gestion des sessions côté serveur tout en garantissant un niveau élevé de sécurité des échanges \cite{jwtRFC}.




\section{Réalisation}

Cette section présente les principales fonctionnalités développées au cours de ce sprint concernant la gestion des utilisateurs et la sécurisation de l'application \ac{MSG}. Elle décrit les différentes interfaces réalisées pour l'inscription des clients, l'authentification des utilisateurs, la réinitialisation des mots de passe ainsi que la création et la gestion des comptes des employés.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Inscription d'un Customer et vérification du compte par e-mail}

Le processus d'inscription permet à un \textbf{Customer} de créer un compte dans l'application en renseignant les informations demandées dans le formulaire d'inscription. Après validation des données, le système enregistre le nouvel utilisateur puis envoie automatiquement un e-mail de confirmation contenant un lien de vérification. Le compte n'est activé qu'après la validation de cette adresse électronique.


\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{images/interfaces/AuthCustomer/RegisterCustomer.png}
    \caption{Interface d'inscription d'un Customer}
    \label{fig:registerCustomer}
\end{figure}
%
%\begin{figure}[H]
%    \centering
%    \includegraphics[width=0.75\textwidth]{images/email_verification.png}
%    \caption{E-mail de vérification du compte}
%    \label{fig:emailVerification}
%\end{figure}
%---------------------------------------------------------

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Authentification des utilisateurs}

L'interface de connexion permet aux différents utilisateurs de l'application (\textbf{Customer}, \textbf{Admin} et \textbf{Employee}) d'accéder à leurs espaces respectifs. Après la saisie des identifiants, Spring Security vérifie les informations de connexion puis génère un jeton JWT permettant d'accéder aux ressources protégées. Selon le rôle de l'utilisateur, celui-ci est automatiquement redirigé vers son interface dédiée.

%---------------------------------------------------------
% Décommenter lorsque les captures seront disponibles
%
\begin{figure}[H]
   \centering
    \includegraphics[width=0.75\textwidth]{images/interfaces/AuthCustomer/LOGINCUSTOMER.png}
    \caption{Interface d'authentification Client}
    \label{fig:loginCustomer}
\end{figure}

\begin{figure}[H]
   \centering
    \includegraphics[width=0.75\textwidth]{images/interfaces/AuthErp/loginERP.png}
    \caption{Interface d'authentification Admin et employee}
    \label{fig:loginErp}
\end{figure}
%
%\begin{figure}[H]
%    \centering
%    \includegraphics[width=\textwidth]{images/role_redirection.png}
%    \caption{Redirection selon le rôle de l'utilisateur}
%    \label{fig:redirectionRole}
%\end{figure}
%---------------------------------------------------------

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Réinitialisation du mot de passe}

L'application offre une fonctionnalité permettant aux utilisateurs de récupérer l'accès à leur compte lorsqu'ils oublient leur mot de passe. L'utilisateur renseigne son adresse e-mail afin de recevoir un lien sécurisé de réinitialisation. Ce lien permet ensuite de définir un nouveau mot de passe.

%---------------------------------------------------------
% Décommenter lorsque les captures seront disponibles
%
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{images/interfaces/AuthCustomer/Forget_Password.png}
    \caption{Interface « Forgot Password <<client>> »}
    \label{fig:forgotPasswordCustomer}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{images/interfaces/AuthErp/Forget_PasswordERP.png}
    \caption{Interface « Forgot Password <<admin et employee>> »}
    \label{fig:forgotPasswordErp}
\end{figure}
%
%\begin{figure}[H]
%    \centering
%    \includegraphics[width=0.75\textwidth]{images/reset_email.png}
%    \caption{E-mail de réinitialisation du mot de passe}
%    \label{fig:resetEmail}
%\end{figure}
%
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{images/interfaces/AuthCustomer/Reset_Password.png}
    \caption{Interface de réinitialisation du mot de passe client}
    \label{fig:resetPasswordClient}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{images/interfaces/AuthErp/Reset_PasswordERP.png}
    \caption{Interface de réinitialisation du mot de passe admin et employee}
    \label{fig:resetPassword}
\end{figure}
%---------------------------------------------------------

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Création et initialisation d'un Employee}

Contrairement au \textbf{Customer}, un \textbf{Employee} ne crée pas son compte lui-même. Celui-ci est créé par l'\textbf{Admin} à partir de l'interface de gestion des employés. Lors de cette création, les informations de connexion sont automatiquement envoyées par courrier électronique. À sa première connexion, l'employé utilise les identifiants reçus afin d'accéder à son espace de travail.

%---------------------------------------------------------
% Décommenter lorsque les captures seront disponibles
%
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/interfaces/AuthErp/CreationEmployee-ERP.png}
    \caption{Interface de création d'un Employee}
    \label{fig:createEmployee}
\end{figure}
%
%\begin{figure}[H]
%    \centering
%    \includegraphics[width=0.75\textwidth]{images/employee_email.png}
%    \caption{E-mail envoyé à l'Employee}
%    \label{fig:employeeEmail}
%\end{figure}
%
%\begin{figure}[H]
%    \centering
%    \includegraphics[width=0.75\textwidth]{images/first_login_employee.png}
%    \caption{Première connexion d'un Employee}
%    \label{fig:firstLoginEmployee}
%\end{figure}
%---------------------------------------------------------

\section{Bilan du Sprint}

Ce premier sprint a permis de mettre en place les fondations de la gestion des
utilisateurs et de la sécurité de l'application MSG. Les principales
fonctionnalités d'authentification, d'inscription, de vérification des comptes,
de gestion des mots de passe et de création des employés ont été réalisées.
La mise en place de Spring Security et du JWT permet également de sécuriser
l'accès aux ressources et d'adapter les autorisations aux différents profils
et départements. Ce socle constitue ainsi une base fiable pour le développement
des fonctionnalités métiers des prochains sprints.